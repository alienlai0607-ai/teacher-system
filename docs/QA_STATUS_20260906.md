# 2026-09-06 更新狀態

## 9/7 正式發布進度（優先於下方歷史狀態）

- 09:45 已更新原 Apps Script 正式部署至第 54 版，網址不變；HTTP ping 實際回傳 `release: 20260906-reliability-1`。第 53 版保留可回復。
- 使用者同意浩浩 Email 綁定延後；未開通、未還原或修改任何人員帳號。
- 使用者指定缺原檔的舊照片先不阻擋系統：僅 2026-09-07 以前的雲端歷史紀錄適用，保留檔名與文字、標示原檔遺失，班務與成果儲存／送出不受阻。新附件缺原檔仍拒絕送出並保留草稿，重新選檔仍須實際上傳。沒有宣稱找回原檔。
- 九組本機回歸通過（新增 legacy-attachment-delivery）；三個工作台語法與 16 個後端模組打包檢查通過。已測歷史附件例外、當日／未知日期不得例外、班務判定一致及補回原檔後清除標記。
- 正式網站已發布：主更新 `f86dd1e`，歷史照片邊界與既有裝置資料相容修正 `19b8b19`、`b71f447`。安親程式快取版本 `20260907-legacy-media-2`，後端／共用 API 版本 `20260906-reliability-1`。
- 09:52:53 使用柏翰正式登入，在正式網站按「實測雲端交付」，取得同次 5/5 通過（18,869ms）：身分 617ms、試算表 3,252ms、安親新增／修改／讀回 3,790ms、照片／私密預覽 6,211ms、教材內容 4,846ms，測試清理均通過。這是由正式前端呼叫驗收 API 的服務往返，不等同逐位老師的全部上傳／送出流程。
- 09:51 透過正式設定頁補齊排程，回讀確認交付重試、備份及既有通知排程已設定。尚未以實際 LINE 推送或等候每個排程首次自動執行驗收。
- 09:53 直接下載正式站六個公開資源，與本機逐位元組相同：安親 JS／HTML、才藝 JS、行政 JS、共用 API、Service Worker，皆 HTTP 200。
- 09:54 正式站重新整理後，既有裝置資料的成果狀態由 29/30 回到 30/30；仍誠實標示 1 個附件無原檔。實際開啟羊羊 IMG_4005.jpeg，主預覽與附件列均顯示「舊附件原檔遺失，不影響儲存或送出」，原有文字保留。沒有按採認證據或更動評分。
- 09:56 正式才藝薪資頁以柏翰帳號讀回真實月結與各老師紀錄，未修改薪資或課堂。
- 正式行政主管頁讀回 29 筆紀錄。本輪額外發現主管姓名寫死為小魚、試上警示錯用獎金內部代碼；已於 `62a5639` 修正並加入實際渲染測試。正式重新整理後顯示柏翰與「待家長決定／追蹤」「已預約／待試上」，仍保留三筆待確認獎金，沒有更改紀錄或獎金。行政 HTML／JS 下載比對均一致。
- 正式排程清單重新整理確認 7 個排程：凌晨備份曾於 03:08:01 執行、交付重試曾於 09:56:20 執行，平台未報執行例外；這不代表實際通知送達率 100%。新建的 6 個排程使用第 54 版，備份使用 HEAD；未來後端發布必須重新核對排程所用版本，不可只更新網頁應用程式。
- 本輪發布及上述驗收完成。仍未驗證每位老師的個人裝置、未綁定帳號、LINE 內建瀏覽器快取及所有角色正式上傳／送出 API；不可宣稱零錯誤或全部情境已完成。LINE 中繼切換、附件原檔完整備援與整站故障改接備份仍未完成，不應混稱為已交付。
- LINE 驗證中繼暫未切換，不阻擋原通知管道；禁止將未啟用的中繼當成已驗收功能。老師未綁定或裝置／LINE 快取情境仍須分別辨識。

## 發布狀態

- 不可宣告更新完成。前端尚未推送，正式 API 尚未切換新版本。
- Apps Script 編輯器已儲存本次待驗收版本；既有排程可能使用編輯器儲存的程式碼，不能說雲端完全未改動。
- LINE 接收網址未切換。Cloudflare `teacher-kpi-line-relay` 已建立，但仍是初始程式，尚未承接 LINE。

## 已確認

- Chrome 已重新啟動，原生介面操作恢復。大型程式碼貼上後須核對搜尋結果與儲存狀態，不可用逐字輸入。
- 八組本機測試通過：api-transport、anqin-task-ui、talent-rules、admin-marketing-rules、production-integrity、reliability-regressions、reliability-secondary、weekend-policy。這些不是八組正式帳號驗收。
- 本輪透過瀏覽器操作，確認 20 組角色入口在 390px／1440px 視窗可開啟，桌面與手機各 134 次分頁導覽沒有空白、破圖或橫向溢出。這不等同所有表單已儲存驗收。
- 驗收發現測試登入會沿用至才藝／行政頁，因此改用另一個本機來源隔開身分後重跑；每個入口都核對畫面上的帳號。不能把同一主管身分的重複檢查當成多位老師驗收。
- 補上隔離安親學生名單回應，仍保留未知 API 明確失敗，避免測試以假成功掩蓋功能缺口。
- 才藝 PT 隔離頁實際選取 16 MB PDF 測試檔與 9.3 MB BMP，確認可多選、BMP 轉成 JPEG、兩附件儲存後重新整理仍保留。PDF 測試檔只用於容量邊界，並非文件內容或成品 PDF 驗證。
- 才藝 PT 無排課日的新增入口先提示「今天沒有已設定的班次」，未讓使用者填完才被拒絕。
- 9/7 PT 紅豆隔離頁實際完成正常排課流程：選備課、填課程問題與下次優化、親師聯繫由待追蹤切回完成（隱藏文字清空且取消必填）、點名照 1 張／成果多選 3 張刪除 1 張／復原照 1 張，儲存草稿後重新整理，再正式送出。送出成功訊息出現，資料讀回仍保留 1／2／1 張附件。
- 同一 PT 課堂再次編輯並儲存，重新整理仍只有一堂、鐘點估算未重複增加；柳丁主管隔離頁可讀到第二版文字。這是前端與隔離資料持久化驗收，不等同正式雲端 PT API 驗收。
- 隔離安親介面：週日免填；空白資料不顯示送出按鈕；自願親師溝通紀錄可送出、顯示成功，重新整理後保留送出時間。
- 隔離介面：週末可進入備課檔案。390px 寬度四步驟各 90px，右側僅 1px 邊框，頁面未橫向溢出。
- 週五晚間不預告安親週六待辦；週六、日不發安親例行待辦提醒；週末不產生例行全員日報推播。週一待辦恢復，既有資料不刪除。以上排程規則已用本機函式測試，未以實際 LINE 傳送驗收。

## 授權與雲端驗收

- 23:12 與 23:13 從編輯器執行驗收，被管理員身分檢查擋下；未進入測試寫入。
- 人員名單 Users!B2 確實存在部署帳號，不能判定使用者登入錯帳號。
- 23:16 的明確診斷：Apps Script manifest 缺少 `https://www.googleapis.com/auth/userinfo.email`，呼叫 Session.getActiveUser 失敗。
- 使用者已明確同意 Email 辨識授權。已在本機及 Apps Script manifest 加入 `userinfo.email`，其餘四項既有 scope、時區與 webapp 設定不變，雲端已儲存。
- 使用者已親自完成 Google 安全確認與授權。23:40 真實雲端驗收 5/5 通過，執行編號 `QA-20260906-234026-299f3e90`，耗時 18,937ms，測試資料已清理。
- 驗收涵蓋管理員身分、試算表寫入讀回、安親紀錄修改、照片預覽、教材內容比對。這是編輯器 HEAD 直接儲存流程驗收，不等同正式前端 API、每位老師登入或 PDF 成品驗收。

## 發布前仍需完成

- 逐角色實際上傳、送出、重新讀取、主管讀取；確認照片與實際 PDF 成品可見。不得以合成預覽資料代替 PDF 成品驗收。
- LINE 中繼驗證、正式接收網址切換與錯誤簽章拒絕測試。
- 交付重試／才藝 PDF 修補排程安裝、附件原檔備援與整站改接備份演練。資料庫備份及隔離還原結果見下方。
- 本機完整瀏覽器端到端關卡、正式版本核對及正式入口複查。本次未執行完整 release-e2e。
- 本機暫存部署工具已移除；發布時仍須確認沒有重新加入。

不能由本次測試推算正式錯誤率，也不能保證所有老師的裝置都已驗證。

## 正式站發現與資料修復

- 正式 Google 登入已成功辨識柏翰管理員，可讀取實際老師資料。但仍是舊前端／API 53 版。
- 主管端實際打開江江「直式加減法」，確認原本只有檔名、無照片。掃描 DailyLogs 已有 187 列，其中 63 個成果附件有 9 個缺連結；不能以「已登記」當成已上傳成功。
- 先建立「KPI系統_附件連結修復前備份_20260906」，確認僅原擁有人可存取，並比對兩筆原始列完整相同。
- 已在正式 DailyLogs 第 173、175 列補回 5 個附件連結：江江 IMG_4297／4298／4300／4301.jpeg；羊羊 IMG_4010.jpeg。僅更新 M 欄快照附件屬性、Q 欄雲端附件清單與 S 欄更新時間，其他欄位逐格比對未變，沒有更改送出或評分狀態。
- 主管正式頁重新整理後，江江主圖已顯示白板照片，四個原檔入口出現；四張縮圖全部讀到實際影像尺寸（1200×1600 或 1600×1200），不是只有檔名。成果視窗內容寬度與捲動寬度均為 965px，未溢出。羊羊「班級公約」的原圖與縮圖也已實際開啟，五個修復附件均確認可見。
- 尚未找回的四檔：小明 S__21667944_0.jpg／S__21667953_0.jpg、羊羊 IMG_4005.jpeg、松鼠 S__110501897_0.jpg。已查目前 Drive、垃圾桶及 Logs_System 11,695 列中的上傳資訊，沒有找到對應原檔或成功上傳紀錄。已向使用者索取原照片，保留既有文字紀錄。
- 本機移除附件回填的「同課程類型即可套用」規則；改成唯一明確對應，並先保留已使用原檔。新增同名多檔、不同課程、既有附件歸屬、已連結原檔與改名案例測試，已通過。
- 本機主管成果統計改稱「成果紀錄」，另外明列原檔待補數量，不再用「已上傳成果」掩蓋只有檔名的歷史紀錄。尚未發布。

## 9/7 凌晨複查

- 真實雲端驗收再次 5/5 通過：`QA-20260907-001725-94ace09b`（20,410ms）、`QA-20260907-002310-582b0239`（25,102ms）。這仍是服務層驗收，不是所有正式送出 API。
- 補上 PDF 更新失敗保護：更新原檔失敗時，保留原檔、原連結與重試機會，不再先丟棄舊 PDF。成功更新後才回收重複檔案。失敗／成功／首次建立三種回歸測試通過；Apps Script HEAD 已儲存，正式 API 仍為 53 版。
- 實際執行 `QA-PDF-20260907-001138`：從江江 9/1、松鼠 9/2 正式紀錄產生私密測試 PDF，下載成品、檢查內嵌影像並逐頁渲染。江江 4 張、松鼠 7 張皆實際顯示，兩份共四頁沒有空白照片。沒有覆寫原日報、更動老師紀錄或傳送 LINE。
- 兩份測試 PDF 已移至垃圾桶；暫時 PDF 檢查程式已從 Apps Script 移除。
- 另下載原本的 `KPI_松鼠_2026-09-02.pdf` 與 `KPI_羊羊_2026-09-02.pdf`，7 張與 14 張影像目前均在雲端原 PDF 內，四頁逐頁渲染確認可見。這兩原檔本輪未修改；沒有驗證老師 LINE 內建瀏覽器的舊快取。
- 正式 Users 名單核對發現：浩浩已啟用但沒有 Google Email；黑豹待開通且無 Email；毛毛與小星星已刪除。未自行開通或變更任何身分。已向使用者索取浩浩登入 Email。
- 00:25 唯讀正式設定檢查：每日 PDF、晨間提醒、晚間預告已設定；交付重試、資料庫備份及才藝 PDF 修補排程未設定；LINE 驗證中繼未啟用；正式操作統計樣本為 0。
- 本機設定頁移除固定為 true 的「上傳成功」類檢查項，改列真實通知憑證與排程設定；不再宣稱「全部完成」，明確區分設定存在與實際交付。回歸測試通過，尚未發布。
- 新版設定頁已在 375px 手機實際開啟、捲動及核對文字：無橫向溢出、重複 DOM ID 或未命名按鈕；隔離資料缺少設定時確實顯示「待設定」，未誤報通過。
- 00:27 正式資料庫備份完成：`KPI備份-2026-09-07-b5c09f27`，18 張資料表的內容與公式逐表比對一致。檔案 ID `14B-_TW6dzysvwf5eGcYij5dTmyVClgx74gxQxXicnL0`，再次查權限確認只有擁有人、沒有公開分享。
- 00:29 從上述備份建立獨立還原副本，實際改寫並恢復一個 DailyLogs 快照儲存格，再比對全部 18 張資料表內容與公式，完全一致。演練副本 `1D0toSPV8QKZgFDzIPeLFj27F-1x1cqkQtVCrnxFV7R4` 已移到垃圾桶；正式資料及保留備份均未改動。這是資料庫副本還原驗證，尚不是整站改接備份或所有照片原檔還原。
- 行政隔離頁另實際驗證每日訊息確認：先選需主管協助、填寫及勾選回報，再切回完成；隱藏備註清空、回報取消勾選、取消必填，儲存成功提示顯示，重新整理仍為已確認。
- 00:31 已建立每日凌晨資料庫備份排程，回讀設定為啟用；原生排程清單確認只有一個備份排程，總共四個排程。三個既有排程畫面顯示錯誤率 0%，但這不代表通知或全部功能成功率 100%；新備份排程尚未到首次自動執行時間。
- 本機部署貼上工具與 PDF 臨時程式已移除，不納入網站發布。雲端暫時設定／備份／還原檢查檔案也已移除，只保留原有 manifest 與正式程式。
